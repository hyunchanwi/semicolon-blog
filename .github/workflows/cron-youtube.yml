name: YouTube Review Cron

on:
  schedule:
    # KST 12:00 = UTC 03:00
    - cron: '0 3 * * *'
  workflow_dispatch:

jobs:
  trigger-youtube:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger YouTube API
        run: |
          curl -X GET ${{ secrets.NEXT_PUBLIC_SITE_URL }}/api/cron/youtube \
          -H "Authorization: Bearer ${{ secrets.CRON_SECRET }}" \
          --fail
